import type { CompletionItem } from 'vscode-languageserver';
import FSProvider from '../../fs-provider';
export declare function componentsContextData(fs: FSProvider, maybeScripts: string[], templateContent: string): Promise<CompletionItem[]>;
