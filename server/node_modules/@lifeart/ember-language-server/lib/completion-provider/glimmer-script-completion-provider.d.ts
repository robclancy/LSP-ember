import { CompletionItem, TextDocumentPositionParams, Position } from 'vscode-languageserver/node';
import Server from '../server';
import ASTPath from '../glimmer-utils';
import { Project } from '../project';
export default class GlimmerScriptCompletionProvider {
    private server;
    constructor(server: Server);
    provideCompletions(params: TextDocumentPositionParams): Promise<CompletionItem[]>;
    transformLegacyResult(result: CompletionItem, scopes: string[], position: Position, focusPath: ASTPath, project: Project): CompletionItem;
}
