import { Location, ReferenceParams } from 'vscode-languageserver/node';
import Server from '../server';
export declare class ReferenceProvider {
    private server;
    constructor(server: Server);
    provideReferences({ textDocument, position }: ReferenceParams): Promise<Location[]>;
}
