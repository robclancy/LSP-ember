import Server from './../server';
import { TextDocumentPositionParams, Definition } from 'vscode-languageserver/node';
import { Project } from '../project';
export default class ScriptDefinitionProvider {
    private server;
    constructor(server: Server);
    handle(params: TextDocumentPositionParams, project: Project): Promise<Definition | null>;
}
