import { Command, CodeAction, CodeActionParams } from 'vscode-languageserver/node';
import Server from '../server';
export declare class CodeActionProvider {
    private server;
    constructor(server: Server);
    provideCodeActions({ textDocument, context, range }: CodeActionParams): Promise<(Command | CodeAction)[] | undefined | null>;
}
